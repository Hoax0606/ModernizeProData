# Sample-data provenance.
#
# Regenerated by:    python3 scripts/gen_sample_data.py
# Schema source:     db/tobe/dev/init.sql (identical across all 4 stages)
# Schema sha256:     4fe756a737d2f00b87ed9d62373e9d11cf4ac35bf482e88cafd1cb2b41d7e959
# Generated on:      2026-05-23
#
# If db/tobe/{stage}/init.sql changes:
#   1. Re-run the generator above.
#   2. Update the sha256 line and date here.
#   3. Wipe + restart stage containers:
#      docker compose -f infra/compose.tobe-stages.yaml down -v
#      docker compose -f infra/compose.tobe-stages.yaml up -d
#
# Per-stage row counts are deterministic (seeded on the stage name) — see
# ROW_COUNTS in scripts/gen_sample_data.py.
